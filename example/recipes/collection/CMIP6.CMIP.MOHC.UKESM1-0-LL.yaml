type: collection

id:
  - method: default
    inputs:
      defaults:
        collection_id: cmip6

extraction_methods:
  - method: json_file
    inputs:
      filepath: example/output/items/
      terms:
        - mip_era
        - activity_id
        - institution_id
        - source_id
        - table_id
        - var_id
        - version
